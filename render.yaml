services:
- type: web
name: my-pdf-tools
env: python
plan: free
buildCommand: "pip install -r requirements.txt"
startCommand: "gunicorn app_fixed:app --bind 0.0.0.0:$PORT"
envVars:
- key: CONVERTAPI_SECRET
value: "<PUT_YOUR_CONVERTAPI_SECRET_HERE>"